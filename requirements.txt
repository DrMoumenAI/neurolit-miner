# NeuroLit Miner — Dependencies
# All other modules (xml.etree, sqlite3, csv, json, os, datetime, argparse,
# collections, itertools) are Python standard library — no installation needed.

requests>=2.28.0      # HTTP calls to NCBI E-utilities and ClinicalTrials.gov API v2
Flask>=2.0.0          # Local web server and REST API
matplotlib>=3.5.0     # V2: publication trend, journal distribution, MeSH figures
numpy>=1.21.0         # V2: trend line regression in analyze.py
